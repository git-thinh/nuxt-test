export const LoadingBar = () => import('../..\\..\\..\\components\\LoadingBar.vue' /* webpackChunkName: "components/loading-bar" */).then(c => wrapFunctional(c.default || c))
export const Mountains = () => import('../..\\..\\..\\components\\Mountains.vue' /* webpackChunkName: "components/mountains" */).then(c => wrapFunctional(c.default || c))
export const MountainsList = () => import('../..\\..\\..\\components\\MountainsList.vue' /* webpackChunkName: "components/mountains-list" */).then(c => wrapFunctional(c.default || c))
export const SocialHead = () => import('../..\\..\\..\\components\\SocialHead.vue' /* webpackChunkName: "components/social-head" */).then(c => wrapFunctional(c.default || c))

// nuxt/nuxt.js#8607
function wrapFunctional(options) {
  if (!options || !options.functional) {
    return options
  }

  const propKeys = Array.isArray(options.props) ? options.props : Object.keys(options.props || {})

  return {
    render(h) {
      const attrs = {}
      const props = {}

      for (const key in this.$attrs) {
        if (propKeys.includes(key)) {
          props[key] = this.$attrs[key]
        } else {
          attrs[key] = this.$attrs[key]
        }
      }

      return h(options, {
        on: this.$listeners,
        attrs,
        props,
        scopedSlots: this.$scopedSlots,
      }, this.$slots.default)
    }
  }
}
