{
  "name": "n2",
  "scripts": {
    "dev": "set NODE_OPTIONS=--openssl-legacy-provider && nuxt --port 12345",
    "build": "set NODE_OPTIONS=--openssl-legacy-provider && nuxt build",
    "gen": "set NODE_OPTIONS=--openssl-legacy-provider && nuxt generate",
    "start": "set NODE_OPTIONS=--openssl-legacy-provider && nuxt start --port 12345"
  },
  "dependencies": {
    "@nuxt/http": "^0.5.1",
    "@nuxtjs/style-resources": "^1.0.0",
    "nuxt": "^2.15.8",
    "sass": "^1.29.0",
    "sass-loader": "^10.1.0",
    "@nuxtjs/axios": "^5.10.3"
  }
}
